export type ArchivedDelivery = {
  deliveryId: string;
  contractId: string;
  contractName: string;
  destination: string;
  pickupLocation: string;
  commodity: string;
  totalScu: number;
  color: string;
};
